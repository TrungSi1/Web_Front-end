const init = {
    cars: ['BMW', 'dream', 'wave']
}

export default function reducer(state = init, action ,args) {
    switch (action) {
          
            default:
                return state
        }
    }
